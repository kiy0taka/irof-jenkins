namespace(lib.FormTagLib).with {
    section(title: 'いろふ') {
        entry(title: 'ConsumerKey', field:'consumerKey') {
            textbox()
        }
        entry(title: 'ConsumerSecret', field:'consumerSecret') {
            textbox()
        }
        entry(title: 'AccessToken', field:'accessToken') {
            textbox()
        }
        entry(title: 'AccessTokenSecret', field:'accessTokenSecret') {
            textbox()
        }
    }
}